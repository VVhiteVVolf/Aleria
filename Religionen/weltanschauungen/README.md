# Weltanschauungen

Künftige Einträge liegen jeweils unter `<id>/eintrag.json`. Den Pfad im Kapitel `weltanschauungen` von `../data/register.json` ergänzen. Die gemeinsame Profilvorlage kann unverändert verwendet werden; die Anleitung steht in `../README.md`.
