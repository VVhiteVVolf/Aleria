# Häresien, Kulte und Sekten

Künftige Einträge liegen jeweils unter `<id>/eintrag.json`. Den Pfad im Kapitel `kulte` von `../data/register.json` ergänzen. Die gemeinsame Profilvorlage kann unverändert verwendet werden; die Anleitung steht in `../README.md`.
